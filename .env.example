# Example environment variables for local development
# Copy this file to .env.local and fill in real secret values. DO NOT commit your .env.local or real secrets to the repo.

# GitHub OAuth / App
GITHUB_SECRET=your_github_secret_here
GITHUB_ID=your_github_id_here

# Google OAuth
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CLIENT_ID=your_google_client_id_here

# NextAuth
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Supabase (public / anon + service role)
NEXT_PUBLIC_SUPABASE_URL=https://your-supabase-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Notes:
# - Never commit real secrets. Keep .env.local in .gitignore.
# - If secrets were committed previously, rotate them immediately (revoke/regenerate).
# - You can copy this to .env.local and replace placeholders before running the app:
#     copy .env.example .env.local  # (PowerShell)
